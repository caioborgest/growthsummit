import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.growthexperience.app',
  appName: 'Growth Experience 2026',
  webDir: 'dist'
};

export default config;
