{
    "name": "growth-summit-root",
    "version": "1.0.0",
    "scripts": {
        "dev": "cd app && npm run dev",
        "build": "cd app && npm run build",
        "start": "cd app && npm run preview"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "@vercel/analytics": "^1.6.1"
    }
}
